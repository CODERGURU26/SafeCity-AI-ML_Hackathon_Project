# 🏗️ SafeCity Backend Architecture

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                     Browser / Client Side                       │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           React Components (Next.js)                     │  │
│  │                                                           │  │
│  │  ┌─────────────────┐      ┌─────────────────┐           │  │
│  │  │  FIR Table      │      │  FIR Filters    │           │  │
│  │  │  (fir-table)    │      │  (fir-filters)  │           │  │
│  │  └────────┬────────┘      └────────┬────────┘           │  │
│  │           │                        │                     │  │
│  │           └────────────┬───────────┘                     │  │
│  │                        │                                  │  │
│  │           ┌────────────▼──────────────┐                  │  │
│  │           │   useFIRData Hook         │                  │  │
│  │           │ (API calls handler)       │                  │  │
│  │           └────────────┬──────────────┘                  │  │
│  └────────────────────────┼──────────────────────────────────┘  │
│                           │                                      │
│                    HTTP Requests/Responses                       │
│                           │                                      │
└───────────────────────────┼──────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                  Next.js API Routes (Backend)                   │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              /api/fir/route.js                           │  │
│  │  ┌─────────────────────────────────────────────────────┐ │  │
│  │  │  GET:  List FIRs with pagination & filtering       │ │  │
│  │  │  POST: Create new FIR                              │ │  │
│  │  └─────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │            /api/fir/[id]/route.js                        │  │
│  │  ┌─────────────────────────────────────────────────────┐ │  │
│  │  │  GET:    Get single FIR by ID                      │ │  │
│  │  │  PUT:    Update FIR details                        │ │  │
│  │  │  DELETE: Delete FIR record                         │ │  │
│  │  └─────────────────────────────────────────────────────┘ │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │         Database Connection Layer                        │  │
│  │                                                          │  │
│  │  ┌─────────────────────────────────────────────────────┐│  │
│  │  │      lib/mongodb.js                                 ││  │
│  │  │  • Connection pooling                               ││  │
│  │  │  • Error handling                                   ││  │
│  │  │  • Cached connections                              ││  │
│  │  └─────────────────────────────────────────────────────┘│  │
│  │                                                          │  │
│  │  ┌─────────────────────────────────────────────────────┐│  │
│  │  │      models/FIR.js (Mongoose Schema)                ││  │
│  │  │  • FIR document structure                           ││  │
│  │  │  • Data validation                                  ││  │
│  │  │  • Indexes                                          ││  │
│  │  └─────────────────────────────────────────────────────┘│  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
└──────────────────────────────────────────────┬──────────────────┘
                                               │
                                    TCP Socket Connection
                                               │
                                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                      MongoDB Atlas Cloud                         │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              safecity Database                           │  │
│  │                                                          │  │
│  │  ┌─────────────────────────────────────────────────────┐│  │
│  │  │          FIRs Collection                            ││  │
│  │  │                                                     ││  │
│  │  │  Document Example:                                 ││  │
│  │  │  {                                                  ││  │
│  │  │    _id: ObjectId("..."),                            ││  │
│  │  │    firId: "FIR-2024-1847",                          ││  │
│  │  │    date: "2024-01-27",                              ││  │
│  │  │    time: "14:32",                                   ││  │
│  │  │    type: "Theft",                                   ││  │
│  │  │    location: "Andheri West",                        ││  │
│  │  │    complainant: "Rajesh Kumar",                     ││  │
│  │  │    status: "open",                                  ││  │
│  │  │    priority: "high",                                ││  │
│  │  │    officer: "SI Patil",                             ││  │
│  │  │    createdAt: ISODate(...),                         ││  │
│  │  │    updatedAt: ISODate(...)                          ││  │
│  │  │  }                                                   ││  │
│  │  │                                                     ││  │
│  │  │  Indexes:                                           ││  │
│  │  │  • firId (unique)                                  ││  │
│  │  │  • status, type, priority (for filtering)          ││  │
│  │  │  • createdAt (for sorting)                         ││  │
│  │  └─────────────────────────────────────────────────────┘│  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Region: [Your Selected Region]                               │
│  Cluster: M0 Free Tier (Development)                           │
│  Backup: Enabled                                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

### 1. **Fetch FIRs with Filters**

```
User Action
    │
    ▼
Component Click
    │
    ▼
useFIRData.fetchFIRs()
    │
    ▼
HTTP GET /api/fir?filters
    │
    ▼
Next.js API Route (route.js)
    │
    ├─ Build MongoDB query
    ├─ Apply filters
    ├─ Set pagination
    │
    ▼
Connect to MongoDB
    │
    ▼
Query: FIR.find(filters).skip().limit()
    │
    ▼
MongoDB processes query
    │
    ▼
Return documents
    │
    ▼
Send JSON response
    │
    ▼
Frontend updates state
    │
    ▼
Component re-renders with new data
```

### 2. **Create FIR**

```
User submits form
    │
    ▼
useFIRData.createFIR(data)
    │
    ▼
HTTP POST /api/fir with JSON body
    │
    ▼
API validation
    │
    ├─ Generate unique FIR ID if needed
    ├─ Validate all fields
    │
    ▼
FIR.create(data)
    │
    ▼
MongoDB inserts document
    │
    ▼
Returns created document
    │
    ▼
Response sent to frontend
    │
    ▼
Success notification + refresh list
```

### 3. **Update FIR**

```
User clicks Edit
    │
    ▼
useFIRData.updateFIR(firId, newData)
    │
    ▼
HTTP PUT /api/fir/:id
    │
    ▼
Find FIR by ID
    │
    ▼
Update fields
    │
    ▼
FIR.findOneAndUpdate()
    │
    ▼
MongoDB updates document
    │
    ▼
Returns updated document
    │
    ▼
Response to frontend
```

---

## Technology Stack

```
Frontend                  Backend                   Database
─────────                ─────────                  ────────
React 19        ──▶     Next.js 16        ──▶     MongoDB Atlas
Next.js                  Node.js                    Mongoose ODM
TypeScript               JavaScript
Tailwind CSS             Express (built-in)
shadcn/ui                REST API
```

---

## API Request/Response Flow

### GET /api/fir

**Request:**

```http
GET /api/fir?page=1&limit=8&search=theft&status=open
```

**Processing:**

1. Parse query parameters
2. Build MongoDB filter object
3. Execute query with skip/limit
4. Get total count for pagination

**Response:**

```json
{
  "success": true,
  "data": [
    {
      "_id": "507f1f77bcf86cd799439011",
      "firId": "FIR-2024-1847",
      "date": "2024-01-27",
      ...
    }
  ],
  "pagination": {
    "total": 45,
    "page": 1,
    "limit": 8,
    "pages": 6
  }
}
```

### POST /api/fir

**Request:**

```json
{
  "date": "2024-01-27",
  "time": "15:30",
  "type": "Theft",
  "location": "Bandra",
  "complainant": "John Doe",
  "status": "open",
  "priority": "high",
  "officer": "SI Patil"
}
```

**Processing:**

1. Validate request body
2. Auto-generate FIR ID
3. Create new document
4. Store in MongoDB

**Response:**

```json
{
  "success": true,
  "data": {
    "_id": "507f1f77bcf86cd799439012",
    "firId": "FIR-2024-1848",
    "date": "2024-01-27",
    "time": "15:30",
    ...
  },
  "message": "FIR created successfully"
}
```

---

## Error Handling Flow

```
API Request
    │
    ▼
[Try to connect MongoDB]
    │
    ├─ Connection Success
    │       │
    │       ▼
    │   [Execute query]
    │       │
    │       ├─ Success ──▶ Return data (200)
    │       │
    │       └─ Query Error ──▶ Return error (400)
    │
    └─ Connection Failed
            │
            ▼
        [MongoDB connection error]
            │
            ▼
        Frontend shows warning
            │
            ▼
        Falls back to sample data
```

---

## Security Considerations

```
┌─────────────────────────────────────────┐
│       Environment Variables              │
├─────────────────────────────────────────┤
│ MONGODB_URI (in .env.local)             │
│ ⚠️  NEVER commit to Git                 │
│ ⚠️  Contains username + password        │
│ ✅ Added to .gitignore                  │
└─────────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│     MongoDB Atlas Network Access         │
├─────────────────────────────────────────┤
│ • IP Whitelist enabled                  │
│ • Only allow 0.0.0.0/0 for development │
│ • Restrict in production                │
│ • Use VPC/firewall rules                │
└─────────────────────────────────────────┘
```

---

## Scalability Path

```
Current Setup (Development)
    │
    ├─ Single M0 cluster
    ├─ Auto-scaling disabled
    ├─ No backups
    └─ Local development only

         ▼▼▼ Scale Up ▼▼▼

Production Setup
    │
    ├─ M2/M5 cluster (4GB+ RAM)
    ├─ Replica sets for high availability
    ├─ Automated backups
    ├─ Connection pooling
    ├─ Read replicas for analytics
    ├─ Indexes on frequently queried fields
    ├─ API rate limiting
    ├─ Caching layer (Redis)
    └─ CDN for static assets
```
